<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movementreminder">

    <application
        android:name="com.example.Database.RealmDbLinkage"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovementReminder">
        <activity android:name=".ViewDatabaseExercises"></activity>
        <activity android:name=".TestingDatabase" />
        <activity android:name=".testingforuserinput" />
        <activity android:name=".AnalysisActivity2" />
        <activity
            android:name=".starRatingPopupActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".ExerciseSelectedActivity" />
        <activity android:name=".PainFormActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>